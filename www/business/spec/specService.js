(function() {
    'use strict';

    angular.module('spec')
        .service('specService', [
            '$rootScope',
            '$q',
            'DataService',
            function($rootScope, $q, DataService) {

            }
        ]);
})();
