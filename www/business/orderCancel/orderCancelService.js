(function() {
    'use strict';

    angular.module('orderCancel')
        .service('orderCancelService', [
            '$rootScope',
            '$q',
            'DataService',
            function($rootScope, $q, DataService) {

            }
        ]);
})();
