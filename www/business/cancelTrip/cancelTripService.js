(function() {
    'use strict';

    angular.module('cancelTrip')
        .service('cancelTripService', [
            '$rootScope',
            '$q',
            'DataService',
            function($rootScope, $q, DataService) {
            }
        ]);
})();
